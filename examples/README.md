# Examples

Behavectl examples are intentionally small and trust-oriented.

- [`agent-native-project`](./agent-native-project/) — what Claude Code / Codex
  integration looks like at repository scope.

For the deterministic end-to-end challenge, use the product itself:

```bash
npx behavectl demo create
```
